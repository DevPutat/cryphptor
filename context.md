# Контекст проекта CRYPHPTOR + LARAVEL

## Основная задача
Заставить Laravel приложение корректно отображаться в браузере при работе с зашифрованными PHP файлами.

## Структура проектов
1. `/home/toffa/projects/cryphptor/` - проект расширения для PHP (Go утилита + PHP расширение)
2. `/home/toffa/projects/test-laravel/` - тестовое Laravel приложение

## Критические ограничения
- НЕЛЬЗЯ изменять файлы Laravel приложения в `/home/toffa/projects/test-laravel/src/`
- МОЖНО изменять только `Dockerfile.crypto` и `docker-compose.yml`
- Все PHP файлы должны быть зашифрованы в контейнере
- Ключ шифрования: `testencryptionkey12345testencrypt`

## Команды сборки
- Сборка расширений: `cd /home/toffa/projects/cryphptor && make docker`
- Сборка Laravel образа: `cd /home/toffa/projects/test-laravel/src && docker build -t laravel-encrypted -f ./Dockerfile.crypto .`

## Сервисы в docker-compose.yml
- app (PHP-FPM): laravel-encrypted, порт 9000
- nginx: nginx:alpine, порт 8000:80
- redis: redis:alpine, порт 6379:6379  
- db: mysql:8.0, порт 3306:3306
- queue: laravel-encrypted, php artisan queue:work
- reverb: laravel-encrypted, php artisan reverb:start
- mailhog: mailhog/mailhog, порты 1025:1025, 8025:8025

## Текущая архитектура расширения
Расширение использует **перехват компиляции PHP файлов** через `zend_compile_file` для дешифровки в памяти:
- **Хук на `zend_compile_file`**: перехватывает все попытки компиляции PHP файлов
- **Автоматическая дешифровка в памяти**: зашифрованные файлы дешифруются при компиляции
- **Без временных файлов**: дешифрованный код существует только в памяти (соответствует требованиям клиента)
- **Автоматическое определение**: файлы в `/var/www/laravel` с расширениями `.php`, `.inc`, `.phtml` проверяются на шифрование

## Техническая проблема
**КЛЮЧЕВАЯ ПРОБЛЕМА**: Laravel читает файлы напрямую через системные функции, игнорируя stream wrapper `cryphptor://`.

### Что происходит сейчас:
1. Laravel читает файлы напрямую (например, `/var/www/laravel/artisan`)
2. Без перехвата системных функций файлы читаются в зашифрованном виде
3. Результат: шифро-текст вместо исполняемого кода
4. PHP интерпретатор получает зашифрованный код и не может его выполнить

### Реализованное решение:
1. **Перехват `zend_compile_file`** при загрузке расширения (в `PHP_MINIT`)
2. **Автоматическое определение зашифрованных файлов** в директории `/var/www/laravel`
3. **Дешифровка в памяти** через PHP stream без создания временных файлов
4. **Прозрачная компиляция** дешифрованного кода через оригинальный компилятор

## Скрипты автоматизации

### build.sh (в cryphptor/)
Полный цикл сборки расширения:
1. `make build` - сборка Go утилит
2. `./dist/phpext_builder` - формирование файлов PHP расширения
3. `make docker-84` - сборка Docker образа cryphptor-phpfpm-8.4
- Все логи сохраняются в `build.log`
- Скрипт прерывается при любой ошибке

### run_laravel.sh (в cryphptor/)
Полный цикл развертывания Laravel:
1. Сборка образа `laravel-encrypted` с зашифрованными файлами
2. `docker compose down` - остановка старых контейнеров
3. `docker compose up -d` - запуск новых контейнеров
- Все логи сохраняются в `/home/toffa/projects/test-laravel/run.log`
- Скрипт прерывается при любой ошибке

## Прогресс решения
- ✅ Расширение собирается и загружается корректно (PHP 8.4)
- ✅ Функция дешифровки работает (проверена в тестах)
- ✅ Хук `zend_compile_file` устанавливается корректно
- ✅ Автоматическое определение зашифрованных файлов в `/var/www/laravel`
- ✅ Дешифровка в памяти без временных файлов (соответствует требованиям клиента)
- ✅ Автоматизация сборки через `build.sh`
- ✅ Автоматизация развертывания Laravel через `run_laravel.sh`
- ⚠️ **Текущая проблема**: расширение загружается, но `zend_compile_file` не вызывается для файлов Laravel

## Текущее состояние
- Расширение успешно загружается в контейнере `laravel_app`
- Хук на `zend_compile_file` установлен (подтверждено в логах PHP)
- Файлы в контейнере зашифрованы (подтверждено через xxd)
- **Проблема**: при запросе к сайту через браузер отображается шифротекст
- **Причина**: возможно, PHP-FPM не вызывает `zend_compile_file` или есть конфликты с opcache

## Цикл проверки расширения
1. Изменение исходного кода в `/home/toffa/projects/cryphptor/internal/phpext/`
2. Запуск `./build.sh` - полная пересборка расширения
3. Запуск `./run_laravel.sh` - пересборка Laravel и перезапуск контейнеров
4. Проверка через `docker exec laravel_app php -r "..."`
5. Проверка в браузере на http://localhost:8000

## Процесс сборки и обновления

### Основной процесс разработки:
1. **Изменение исходного кода** в `./internal/phpext/`
2. **Сборка builder'а**: `make build`
3. **Копирование файлов**: `./dist/phpext_builder`
4. **Сборка Docker образа**: `make docker-84`

### Важно: Порядок команд критичен!
- Нельзя изменять файлы напрямую в `./dist/phpext/` - они будут перезаписаны
- Всегда сначала `make build`, потом `./dist/phpext_builder`
- Только после этого можно запускать `make docker-84`

## Текущий статус и проблемы

### ✅ Что работает:
1. Расширение успешно собирается без ошибок компиляции
2. Dockerfile.phpfpm-8.4 корректно создает образ
3. Laravel образ успешно создается с зашифрованными файлами
4. Все контейнеры запускаются без ошибок Docker

### ✅ РЕШЕНО: Расширение успешно загружается!

**Решение проблемы:**
- Проблема была в том, что макрос `COMPILE_DL_CRYPTHPTOR` не экспортировал символы
- Заменено на прямую функцию `get_module()` 
- Расширение теперь корректно загружается в PHP

**Проверка:**
```bash
php -r "var_dump(extension_loaded('cryphptor'));"
# Результат: bool(true)
```

### Следующие шаги для решения:
1. **Исследовать правильные пути к OpenSSL в PHP контейнере**
2. **Проверить другие PHP расширения, использующие OpenSSL**
3. **Исправить config.m4 с правильными путями и библиотеками**
4. **Протестировать загрузку расширения**
5. **Проверить дешифровку файлов в Laravel**

## КРИТИЧЕСКОЕ ТРЕБОВАНИЕ КЛИЕНТА 🔴
**В контейнере НЕЛЬЗЯ хранить расшифрованный код!**
- Пользователи с read-доступом к sh в контейнере НЕ должны видеть исходный код
- Расширение должно дешифровывать файлы **в памяти** в момент интерпретации
- **НИКАКИХ временных файлов на диске** - только дешифровка в RAM
- **НИКАКИХ скриптов дешифрации при старте контейнера**
- Шифр может быть простым для скорости, но все PHP файлы должны быть зашифрованы
- Расшифровка происходит ТОЛЬКО в момент выполнения PHP интерпретатором

## Исправление шифрования (ВАЖНО!)
**Исправлено**: encryptor теперь копирует ВСЕ файлы, не только PHP:
- PHP файлы (.php, .inc, .phtml) шифруются
- Все остальные файлы (.env, artisan, composer.json, vendor/, и т.д.) копируются в открытом виде
- Это критично для работы Laravel - artisan, .env, composer.json и всё в vendor/ теперь присутствуют в контейнере

## Потенциальные проблемы
1. ❌ ВРЕМЕННЫЕ ФАЙЛЫ НЕ ДОПУСТИМЫ - текущий подход с созданием временных файлов нарушает требования клиента
2. Возможные конфликты с другими расширениями при перехвате системных функций
3. Производительность дешифровки при каждом чтении файла (но это допустимо)
4. Nginx ищет файлы в `/var/www/laravel/public`, но контейнер app копирует в `/var/www/laravel`