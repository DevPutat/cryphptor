; Configuration for xor_decrypt extension

; The XOR decryption key used to decrypt PHP files at runtime
; This key must match the key used during encryption
; Example: xor_decrypt.key = "my_secret_key_123"
xor_decrypt.key = ""

; Enable or disable the extension globally
; Default is On when extension is loaded
xor_decrypt.enable = On
